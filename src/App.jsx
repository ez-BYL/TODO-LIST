// Import React and the TodoList component
import React from 'react';
import TodoList from './TodoList';

// Define the App component
function App() {
  // Render the TodoList component
  return <TodoList />;
}

// Export the App component as the default export
export default App;